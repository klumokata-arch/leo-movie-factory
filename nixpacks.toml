[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/python -c \"import imageio_ffmpeg; print('ffmpeg binary:', imageio_ffmpeg.get_ffmpeg_exe())\""]

[start]
cmd = ".venv/bin/python main.py"
