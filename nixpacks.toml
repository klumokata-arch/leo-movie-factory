[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["python -m venv .venv && .venv/bin/pip install -r requirements.txt"]

[start]
cmd = "ffmpeg -version && .venv/bin/python main.py"
